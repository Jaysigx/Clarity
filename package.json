{
  "name": "clarity",
  "version": "0.1.0",
  "private": true,
  "description": "AI-native IDE foundation",
  "workspaces": [
    "apps/webview",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "demo": "npm run build && node ./apps/webview/dist/apps/webview/src/demo.js",
    "ui": "npm run build && npm run serve --workspace @clarity/webview",
    "serve": "npm run build && npm run serve --workspace @clarity/webview",
    "electron": "npm run build && cd apps/desktop && npm install && npx electron .",
    "desktop": "npm run build && cd apps/desktop && npm run dev",
    "desktop:dev": "npm run build && cd apps/desktop && npm run dev",
    "desktop:build": "cd apps/desktop && npm run build",
    "desktop:dist": "cd apps/desktop && npm run dist"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "typescript": "^5.9.3"
  }
}
